<Window x:Class="WpfLibrary1.OfficersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сотрудники" Height="500" Width="941" WindowStartupLocation="CenterOwner" Background="#0B2545" Foreground="#FFFFFF"
        ResizeMode="CanResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="OfficersGrid" AutoGenerateColumns="False" IsReadOnly="True" Margin="12,12,10,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" />
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" />
                <DataGridTextColumn Header="Отчество" Binding="{Binding Patronymic}" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" />
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
            <Button x:Name="AddButton" Width="140" MinHeight="32" Margin="0,0,8,0" Click="OnAdd" Height="31">Добавить</Button>
            <Button x:Name="EditButton" Width="140" MinHeight="32" Margin="0,0,8,0" Click="OnEdit" Height="31">Изменить</Button>
            <Button x:Name="DeleteButton" Width="140" MinHeight="32" Margin="0,0,8,0" Click="OnDelete" Height="27">Удалить</Button>
            <Button x:Name="UnassignButton" Width="160" MinHeight="32" Margin="0,0,8,0" Click="OnUnassign" Height="32">Убрать из отдела</Button>
            <Button Width="120" MinHeight="32" Click="OnClose">Готово</Button>
        </StackPanel>
    </Grid>
</Window>